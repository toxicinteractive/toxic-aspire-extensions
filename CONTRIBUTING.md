# Toxic.Aspire.Extensions

## How to push a new version
1. Update the version number in Toxic.Aspire.Extensions.csproj. The major version number of the package version aligns with the corresponding Aspire version.
2. Create a git tag with the version number
3. Run the publishing workflow in GitHub
